# Project Phoenix v1.1.1 Hotfix

Fixes a JavaScript syntax error in v1.1 that prevented the app from loading. Also increments the offline cache version so installed devices fetch the corrected files.

Upload all files to the existing GitHub repository and commit directly to main. Vercel will redeploy automatically.
